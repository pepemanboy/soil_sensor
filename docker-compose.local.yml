# Local / LAN: app only on port 3000 (no Caddy).
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build app

services:
  caddy:
    profiles:
      - never

  app:
    ports:
      - "127.0.0.1:3000:3000"
